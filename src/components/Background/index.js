import styled from "styled-components";

const BackgroundGradient = styled.div`
  position: fixed;
  z-index: -101;
  height: 100%;
  width: 100%;

  background-image: linear-gradient(30deg, lightblue, cadetblue, lightblue);
`;
export default BackgroundGradient;
